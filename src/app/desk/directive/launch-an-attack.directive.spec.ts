import { LaunchAnAttackDirective } from './launch-an-attack.directive';

describe('LaunchAnAttackDirective', () => {
  it('should create an instance', () => {
    const directive = new LaunchAnAttackDirective();
    expect(directive).toBeTruthy();
  });
});
